# Supabase — public client values (safe to expose to browser).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Backend API base. Leave empty to use the Vite dev proxy (/api).
VITE_API_BASE=
